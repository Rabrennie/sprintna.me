// import {describe, expect, test, jest} from '@jest/globals';
// import short, { SUUID } from 'short-uuid';
// import Room from './Room';

// jest.mock('short-uuid')

// const mshort = short as jest.Mocked<typeof short>;
// mshort.generate.mockImplementation(() => 'wowza' as SUUID);

// describe('Room model', () => {
//     test('sets a unique id', () => {
//         const room = new Room('test');

//         expect(room.id).toEqual('wowza');
//     });
// });
console.log('yo');
